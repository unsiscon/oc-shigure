# opco-shigure 项目交接文档（2026-08-03）

> **本文档用途**：原对话因上下文过大（62 万 token）导致 opencode 进程多次被系统 kill，现移交新对话承接。
> **读本文档的新对话**：请完整阅读本文件 + 下方列出的关键文件，即可无缝承接工作。
>
> **⚠️ 接手 Agent 职责边界（强制）**：接手本项目的 agent **不负责操作或执行其他 opencode 进程**，只允许**监视**其他 opencode 进程的运行情况（如查看 boulder.json、文件时间戳、进程状态等只读操作），**禁止**向其他 opencode 进程发送指令、修改其文件、重启/终止其进程或以任何方式影响其他 opencode 进程的执行。任何需要与其他 opencode 进程交互的操作，必须先征得用户确认。

---

## 1. 项目是什么

**opco-shigure**：一个原生嵌入 OpenCode TUI 侧栏、使用 ANSI 半块（▀▄█）高密度像素呈现"时雨"宠物、实时反映当前 Agent 会话状态的陪伴插件。

- 项目路径：`/Users/unsis/code/opcode/opcopet`
- 技术栈：TypeScript + tsup(ESM) + vitest + Solid.js + OpenTUI（`@opentui/core`/`@opentui/solid`/`@opentui/keymap`）
- 目标平台：OpenCode 1.18+ 原生 TUI 插件（`sidebar_content` slot）
- 设计文档：`docs/` 下 7 篇（01 调研 / 02 需求 / 03 状态模型 / 04 架构 / 05 视觉规范 / 06 质量验收 / 07 视觉交接）

## 2. 当前进度（关键！）

### 2.1 规划阶段：✅ 已完成并通过双审

- 计划文件：`.omo/plans/opco-shigure-tui-skeleton.md`（54KB，SHA `7f1d64fa…`）
- 草稿/评审台账：`.omo/drafts/opco-shigure-tui-skeleton.md`
- 规划经过了 **5 轮高精度双审**（Momus + Oracle），33 项评审发现全部折入计划，最终双路 APPROVE

### 2.2 执行阶段：todo 1-7 ✅ 完成，todo 8 🔄 代码已完成但状态未标记

**boulder.json 记录**（`.omo/boulder.json`）：

| todo | 内容 | 状态 |
|---|---|---|
| 1 | 包骨架 + 领域类型 + 配置层（TDD） | ✅ completed |
| 2 | SDK 事件适配器（TDD） | ✅ completed |
| 6 | 半块渲染器（TDD） | ✅ completed |
| 7 | 会话控制器注册表 + Solid 侧栏组件 | ✅ completed |
| **8** | TUI 入口 + 命令 + 生命周期 + 构建/打包/真实冒烟 + README | 🔄 **代码已完成，状态仍为 running** |
| 3/4/5 | （计划中的其他 todo，见计划文件） | 状态需确认 |

### 2.3 todo 8 的实际完成情况（代码层面已验证）

```bash
cd /Users/unsis/code/opcode/opcopet
npx vitest run    # ✅ 135/135 通过（7 个测试文件）
npx tsc --noEmit  # ✅ 0 错误
```

- `src/` 下 20 个文件全部实现：adapter / reducer / animation / manifest / renderer / config / registry / sidebar / tui + 各自测试
- `dist/tui.js` 构建产物已生成（48KB）
- `opco-shigure-0.1.0.tgz` npm 打包产物已生成
- **todo 8 的 worker 从未卡死**：11:49 派发 → 12:27 npm install → 12:55 tui.ts → 12:59 sidebar.tsx → 13:17 registry.ts + dist 重建，一直在工作。之前"卡住"是会话可见性问题（worker 走独立路径，不显示在 Atlas 主会话 part 记录里）

## 3. 下一步工作

### 3.1 恢复执行（重要）

```bash
# 在 opencode 会话中输入（Atlas 会自动读 boulder.json 恢复）
/start-work opco-shigure-tui-skeleton
```

- boulder.json 已有完整 task_sessions 记录，Atlas 会从 todo 8 继续
- 注意 todo 8 代码已完成，但状态是 running——恢复后 Atlas 应先验证代码再标记 completed

### 3.2 剩余工作

1. **todo 8 收尾**：标记 completed（代码已在，需验证构建+打包+README）
2. **F1-F4 终验**（计划文件的验收波次）：
   - F1 计划合规审计
   - F2 代码质量评审
   - F3 独立真实 TUI 手工 QA（真实 OpenCode TUI 冒烟）——**需要真实配置安装 + 可能耗 token**
   - F4 范围保真
3. 最终美术资产（占位帧替换，README 已说明交接）

## 4. 环境与配置状态

### 4.1 omo 插件配置（`~/.omo/omo.jsonc`）

当前模型路由（**全部低成本方案**）：

| 代理 | 模型 |
|---|---|
| sisyphus / prometheus / atlas / junior / metis / oracle / explore | `opencode-go/deepseek-v4-flash` |
| momus | `openai/gpt-5.6-luna` (high) |
| librarian | `opencode-go/qwen3.7-plus` |
| 禁用 | hephaestus, multimodal-looker, ultrabrain（category 禁用） |

### 4.2 关键备份位置

```
~/.opencode-backups/
├── opco-shigure-plan-flash-20260802-224628/   # ds 轮计划备份（plans 44KB + drafts 12KB）
└── opco-shigure-k27-20260802-233659/          # k2.7 轮完整备份（配置+计划+会话+数据库 101MB）
```

### 4.3 其他配置

- opencode 全局插件：`~/.config/opencode/opencode.jsonc` 注册了 `oh-my-openagent@latest`
- 项目级：`/Users/unsis/code/autumn-chat/.opencode/opencode.json` 有 `subagent_depth: 2`（autumn-chat 项目，与 opco-shigure 无关）

## 5. 待办事项（用户明确要求）

> **更新（2026-08-03）**：以下第 1-4 项（删除 autumn-chat 自定义子代理架构）已由新对话全部执行完成 ✅，详见 5.1。

### 5.1 删除 autumn-chat 自定义子代理架构（✅ 已完成）

- 已删除：`/Users/unsis/code/autumn-chat/.opencode/agent/`（coder/leader/reviewer/reviewer-strict/verifier/visual 共 6 个自定义 agent）
- 已删除：`/Users/unsis/code/autumn-chat/.opencode/notepads/`（learnings.md）
- 已删除：`/Users/unsis/code/autumn-chat/docs/子代理架构设计.md`
- 备份位置：`~/.opencode-backups/custom-agents-20260803/`（含 agent/、notepads/、子代理架构设计.md，备份完整）
- 保留：`.opencode/opencode.json`（`subagent_depth: 2`）及 `.opencode/` 下的 node_modules/package.json（插件依赖），由用户决定是否进一步清理

### 5.2 接手 Agent 职责边界（强制约束）

接手本项目的 agent 必须遵守以下边界：

1. **不操作、不执行其他 opencode 进程**：禁止向其他 opencode 会话/进程注入指令、发送输入、修改其会话文件、调用其工具。
2. **只允许监视**：可以只读方式查看其他 opencode 进程的运行情况（boulder.json、日志、文件时间戳、进程列表等），用于了解进度和状态。
3. **不干预执行**：不得重启、终止、暂停、信号中断其他 opencode 进程，不得修改其工作目录下的文件。
4. **如需交互**：任何可能影响其他 opencode 进程的操作，必须先向用户说明并获得明确确认，否则一律不做。

## 6. 重要注意事项（踩过的坑）

### 6.1 内存问题（严重）

- **原对话上下文达到 62 万 token（62% used）后，opencode 进程被 macOS 系统 kill 多次**
- 16GB MacBook Air 上，opencode + MCP×4 + LSP + 多子代理，内存峰值会撞墙
- **教训：大任务拆多个 session；单会话上下文控制在 20-30 万 token 以下；定期 `/stop-continuation`**

### 6.2 余额问题

- opencode-go 曾出现 `Insufficient Balance` 错误（大请求触发，小请求/缓存命中仍可用）
- 当前实测 API 可用（deepseek-v4-flash、kimi-k2.7-code 均正常）
- 若再遇余额错误：中断等充值，或临时切换提供商（deepseek API key / openai 订阅可用）

### 6.3 模型对比结论（研究记录）

- v4-flash vs kimi-k2.7 作为 Prometheus 的对比：双审前 v4-flash 面试更细（3 次 question），k2.7 用默认选择表；双审闭环两者都能完整执行（v4-flash 完整走完 5 轮双审）
- 成本上 v4-flash 远低于 k2.7（0.14/0.28 vs 0.95/4）
- omo 官方文档警告 DeepSeek 不适合编排角色（写于 2026-06-14，早于 deepseek 0731 正式版，对 V4 无定论），但实际运行 v4-flash 在规划/编排中表现可用

### 6.4 其他已知问题

- 会话可见性：worker 执行时不显示在 Atlas 主会话 part 记录里，判断进度要看文件时间戳或 boulder.json
- 空消息问题：网络中断可能产生空 assistant 消息导致 provider 报错（"message at position N must not be empty"），处理方式是删掉该空消息记录
- telemetry：omo 默认开匿名遥测（一天一次），用户决定不管

## 7. 快速验证命令

```bash
cd /Users/unsis/code/opcode/opcopet
npx vitest run          # 测试（应 135/135）
npx tsc --noEmit        # 类型检查（应 0 错误）
npx tsup                # 构建（生成 dist/tui.js）
cat .omo/boulder.json   # 执行状态
```

---

**交接完毕。新对话请从第 2 节（进度）和第 3 节（下一步）开始。**
