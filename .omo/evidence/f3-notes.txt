opco-shigure F3 — REAL MANUAL QA notes (independent rerun of todo 8 smoke)
==========================================================================
Date: 2026-08-03 14:02-14:04 | Verifier: independent of todo 8 implementer
Stack: python pty (TIOCSWINSZ 200x50, TERM=xterm-256color, COLORTERM=truecolor)
  -> opencode 1.18.10 (/opt/homebrew/bin/opencode), real ~/.config/opencode/tui.json
     with absolute-path entry "/Users/unsis/code/opcode/opcopet/dist/tui.js" (retained).
Driver: .omo/evidence/f3-smoke.py (copy of task-8-smoke.py; only output file
  -> f3-smoke.typescript and task file -> f3-hello.txt changed; all steps S1-S17
  identical, 30s per-step timeouts).
Evidence: .omo/evidence/f3-smoke.typescript (raw ANSI stream + screen snapshots).

1. RESULT SUMMARY — 23/23 PASS (identical to todo 8: 23/23)
------------------------------------------------------------
  S1  tui-up              PASS  home screen rendered
  S2  task-complete       PASS  real task: "write .omo/evidence/f3-hello.txt"
                                -> Done. visible; f3-hello.txt created (then cleaned up)
  S3  provider-task-states PASS  labels seen: 思考 (0.4s) -> 完成 (8.3s) -> 待机 (10.8s)
                                (t8: 0.4s / 8.2s / 10.5s — consistent)
  S5  pet-render-idle     PASS  11 block rows / 179 block chars at assertion;
                                snapshot evidence: 332 block chars / 12 block rows
                                (col>=140 sidebar region), sample rows:
                                  ▄▄▀▀▀▀▀▀▀▀▀▀▀▀▄▄
                                  ████▀▀▀▀▀▀▀▀▀▀▀▀████
                                  ██████▀▀████▀▀██████
  S6  idle-label          PASS  待机 at row 31 col 177 (t8: row 30 col 177 —
                                one-row offset from session header, same sidebar region)
  S7  order-600           PASS  builtin rows: Context=4, MCP=9, LSP=16; pet top row=20
                                (t8: 3/8/15/19 — same +1 row offset, pet STACKED AFTER
                                builtins in both runs -> order 600 effective)
  S4  waiting-trigger     PASS  等待 at 3.6s after "ask me a question" prompt
                                (t8: 4.6s); answered "n"
  S8  cmd-hide            PASS  block rows after hide = 0; kv enabled=False
  S9  cmd-show            PASS  block rows restored = 11; kv enabled=True
  S10 cmd-compact         PASS  max 12 block chars/row (16x8 grid); kv size=compact
  S11 cmd-regular         PASS  max 20 block chars/row (24x12 grid); kv size=regular
  S12 cmd-anim-off        PASS  kv animations=False; pet STILL rendered (FR-4 label stays)
  S13 cmd-anim-on         PASS  kv animations=True
  S14 final-state         PASS  pet visible + 待机 label
  S15 resize-reflow       PASS  200x50 -> 120x40 -> 200x50; pet re-rendered
  S16 theme-switch        PASS  palette -> light theme, pet rendered, restored to dark
  S17 load-evidence       PASS  plugin-meta load_count 60 -> 61 (source=file,
                                fingerprint=dist/tui.js); [opco-shigure] debug line
                                present in stream (1 line)

2. KEY ASSERTIONS vs F3 SPEC (all PASS)
---------------------------------------
  - Plugin load: authoritative via captured pet render (▀▄█ block rows in sidebar)
    + plugin-meta load_count increment 60->61. Assertion 1 PASS.
  - 待机 label below pet in sidebar: PASS (S6, row 31 col 177, col>=140 region).
  - Sidebar pet stacked AFTER builtin Context/MCP/LSP: PASS (S7, pet_top=20 > max
    builtin row 16). Assertion 3 PASS.
  - Real task observable: PASS — 思考/完成/待机 observed (S2/S3). "工作" not
    observable on 1.18.10 (TUI bus does not deliver session.next.*; independent
    event probe in t8 recon). Recorded honestly, NOT masked: working semantics
    covered by unit tests S01-S15 (src/reducer.test.ts / registry.test.ts).
  - Three commands: Hide -> Show -> Compact -> Regular -> Animations off (label
    still rendered, FR-4) -> on: ALL PASS (S8-S13), kv verified each step.

3. ADVERSARIAL RECORD
---------------------
  - misleading_success_output: every assertion grounded in the captured ANSI
    stream / reconstructed screen (block rows, label coordinates, kv file),
    not in process exit codes or stderr logs.
  - hung commands: none — every step completed within its 30s timeout
    (no step FAIL, no timeout record in typescript).
  - cleanup: .omo/evidence/f3-hello.txt removed after smoke (verified absent);
    t8's hello.txt still absent; kv left in final state
    (enabled=True, size=regular, animations=True — same as t8 end state).
  - config: ~/.config/opencode/tui.json untouched, opco-shigure entry retained
    (["oh-my-openagent@latest", "/Users/unsis/code/opcode/opcopet/dist/tui.js"]).
  - no src/, docs/, design/ files touched; no plan checkboxes modified.

4. CONSISTENCY WITH TODO 8
--------------------------
  All 23 assertions PASS in both runs with equivalent numeric evidence
  (label timing within ~0.3-1.0s, block rows 11/179, pet stacked after builtins,
  kv transitions identical). Known limitation identical: 工作 state not
  runtime-observable on opencode 1.18.10 (see task-8-fallback.txt item 1).

VERDICT: PASS
